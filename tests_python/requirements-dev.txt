# Dependências só dos testes do sidecar (o sidecar em si é stdlib puro).
pytest>=7.0
# GATE cross-runtime da Descoberta profunda (H7, DEC-HX-005): o motor
# release/python/motor_segmentos.py é tier full (numpy). Sem numpy os testes de
# paridade são PULADOS (importorskip) — o CI instala para o GATE rodar de verdade.
numpy>=1.24
